-- ============================================
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
 
 
 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
 
 
 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
 
 
 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
-- ADD GROUP COLUMNS TO ITINERARIES TABLE
-- Run this in Supabase SQL Editor
-- This adds group_id and group_name support for itinerary grouping
-- ============================================

-- Add group_id column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_id TEXT;

-- Add group_name column to itineraries table (if not exists)
ALTER TABLE public.itineraries 
ADD COLUMN IF NOT EXISTS group_name TEXT;

-- Create index for better performance on group queries
CREATE INDEX IF NOT EXISTS idx_itineraries_group_id ON public.itineraries(group_id);

-- Create index for group name searches
CREATE INDEX IF NOT EXISTS idx_itineraries_group_name ON public.itineraries(group_name);

-- Success message
SELECT 'Group columns added to itineraries table successfully!' as result; 
 
 
 
 