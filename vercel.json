{
  "buildCommand": "cd apps/desktop/renderer && npm ci --no-audit --no-fund --workspaces=false && npm run build",
  "outputDirectory": "apps/desktop/renderer/dist",
  "framework": null,
  "installCommand": "npm ci --no-audit --no-fund --workspaces=false"
}

