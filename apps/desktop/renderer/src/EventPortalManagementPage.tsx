        {/* Launch Event & Preview Timeline - Two Columns */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 32, marginBottom: 48 }}>
          {/* Launch Event Section */}
          <div style={getSectionStyles(isDark)}>
            <h2 style={{ fontSize: 24, fontWeight: 700, marginBottom: 24, borderBottom: `2px solid ${isDark ? '#333' : '#e5e7eb'}`, paddingBottom: 12 }}>
              Launch Event
            </h2>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 24 }}>
              <p style={{ fontSize: 16, color: isDark ? '#ccc' : '#666', margin: 0 }}>
                Launch your event to make it live for all guests and participants.
              </p>
              <button
                style={{
                  borderRadius: 8,
                  padding: '10px 8px',
                  fontSize: 13,
                  fontWeight: 600,
                  cursor: 'pointer',
                  transition: 'all 0.2s ease',
                  display: 'inline-flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: 4,
                  width: 100,
                  height: 36,
                  background: isDark 
                    ? 'rgba(255, 255, 255, 0.1)' 
                    : 'rgba(255, 255, 255, 0.2)',
                  backdropFilter: 'blur(8px)',
                  border: isDark 
                    ? '1px solid rgba(255, 255, 255, 0.2)' 
                    : '1px solid rgba(0, 0, 0, 0.1)',
                  color: isDark ? '#ffffff' : '#000000',
                }}
              >
                Launch Event
              </button>
            </div>
          </div>

          {/* Preview Timeline Section */}
          <div style={getSectionStyles(isDark)}>
            <h2 style={{ fontSize: 24, fontWeight: 700, marginBottom: 24, borderBottom: `2px solid ${isDark ? '#333' : '#e5e7eb'}`, paddingBottom: 12 }}>
              Preview Timeline
            </h2>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 24 }}>
              <p style={{ fontSize: 16, color: isDark ? '#ccc' : '#666', margin: 0 }}>
                Preview the event timeline as your guests will see it before launching.
              </p>
              <button
                style={{
                  borderRadius: 8,
                  padding: '10px 8px',
                  fontSize: 13,
                  fontWeight: 600,
                  cursor: 'pointer',
                  transition: 'all 0.2s ease',
                  display: 'inline-flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: 4,
                  width: 100,
                  height: 36,
                  background: isDark 
                    ? 'rgba(255, 255, 255, 0.1)' 
                    : 'rgba(255, 255, 255, 0.2)',
                  backdropFilter: 'blur(8px)',
                  border: isDark 
                    ? '1px solid rgba(255, 255, 255, 0.2)' 
                    : '1px solid rgba(0, 0, 0, 0.1)',
                  color: isDark ? '#ffffff' : '#000000',
                }}
              >
                Preview Timeline
              </button>
            </div>
          </div>
        </div>

        // ... existing code ... 
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 32, marginBottom: 48 }}>
          {/* Launch Event Section */}
          <div style={getSectionStyles(isDark)}>
            <h2 style={{ fontSize: 24, fontWeight: 700, marginBottom: 24, borderBottom: `2px solid ${isDark ? '#333' : '#e5e7eb'}`, paddingBottom: 12 }}>
              Launch Event
            </h2>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 24 }}>
              <p style={{ fontSize: 16, color: isDark ? '#ccc' : '#666', margin: 0 }}>
                Launch your event to make it live for all guests and participants.
              </p>
              <button
                style={{
                  borderRadius: 8,
                  padding: '10px 8px',
                  fontSize: 13,
                  fontWeight: 600,
                  cursor: 'pointer',
                  transition: 'all 0.2s ease',
                  display: 'inline-flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: 4,
                  width: 100,
                  height: 36,
                  background: isDark 
                    ? 'rgba(255, 255, 255, 0.1)' 
                    : 'rgba(255, 255, 255, 0.2)',
                  backdropFilter: 'blur(8px)',
                  border: isDark 
                    ? '1px solid rgba(255, 255, 255, 0.2)' 
                    : '1px solid rgba(0, 0, 0, 0.1)',
                  color: isDark ? '#ffffff' : '#000000',
                }}
              >
                Launch Event
              </button>
            </div>
          </div>

          {/* Preview Timeline Section */}
          <div style={getSectionStyles(isDark)}>
            <h2 style={{ fontSize: 24, fontWeight: 700, marginBottom: 24, borderBottom: `2px solid ${isDark ? '#333' : '#e5e7eb'}`, paddingBottom: 12 }}>
              Preview Timeline
            </h2>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 24 }}>
              <p style={{ fontSize: 16, color: isDark ? '#ccc' : '#666', margin: 0 }}>
                Preview the event timeline as your guests will see it before launching.
              </p>
              <button
                style={{
                  borderRadius: 8,
                  padding: '10px 8px',
                  fontSize: 13,
                  fontWeight: 600,
                  cursor: 'pointer',
                  transition: 'all 0.2s ease',
                  display: 'inline-flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  gap: 4,
                  width: 100,
                  height: 36,
                  background: isDark 
                    ? 'rgba(255, 255, 255, 0.1)' 
                    : 'rgba(255, 255, 255, 0.2)',
                  backdropFilter: 'blur(8px)',
                  border: isDark 
                    ? '1px solid rgba(255, 255, 255, 0.2)' 
                    : '1px solid rgba(0, 0, 0, 0.1)',
                  color: isDark ? '#ffffff' : '#000000',
                }}
              >
                Preview Timeline
              </button>
            </div>
          </div>
        </div>

        // ... existing code ... 